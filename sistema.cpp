#include <iostream>
#include <string>
#include <vector>
#include "sistema.h"

using namespace std;

string Sistema::Usuarios(){
  for(int i = 0; i < 5; i++){
  return (usuarios[i]);
 }
 return 0;
}

string Sistema::Servidores(){
  for(int i = 0; i < 5; i++){
  return (servidores[i]);
 }
 return 0;
}