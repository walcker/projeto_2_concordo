#ifndef CANALTEXTO_H
#define CANALTEXTO_H

using namespace std;

class Canaltexto {
  public:
string nome;
//vector<string> vetor; 
vector<string> mensagens;  
vector<string> mensagensCanal;                              ;

string Nome();

string Mensagens();
string MensagensCanal();

};

#endif